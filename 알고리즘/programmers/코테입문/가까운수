'''
정수 배열 array와 정수 n이 매개변수로 주어질 때, 
array에 들어있는 정수 중 n과 가장 가까운 수를 return 하도록 
solution 함수를 완성해주세요.
'''

import sys

array = [10, 11, 12]
n = 13

def solution(array, n):
    answer = 0
    temp = 9999
    for i in array:
        gap = abs(n-i)
        if gap < temp:
            temp = gap
            answer = i
        elif gap == temp:
            answer = min(answer, i)
            
    return answer

print(
    solution(array, n)
)