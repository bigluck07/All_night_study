<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">

    <select id="login" resultType="MemberDTO">
		select *
		from shop
		<if test="searchName =='userid' and searchPass =='passwd'">
	       where userid = searchValue and passwd=searchName
	    </if>
		order by userid
	</select>
	
	<select id="idCheck" resultType="int" parameterType="string">
		select count(userid)
		from member
		where userid=#{userid}
	</select>
	
	<insert id="memberAdd" parameterType="MemberDTO">
		insert into member (userid, passwd, username, post, addr1, addr2, phone1, phone2, phone3, email1, email2)
    	values( #{userid}, #{passwd}, #{username}, #{post}, #{addr1}, #{addr2}, #{phone1}, #{phone2}, #{phone3}, #{email1}, #{email2})
    </insert>
   
    <select id="mypage" >
	
    </select>
   
    <update id="updateMember">
	
    </update>
</mapper>